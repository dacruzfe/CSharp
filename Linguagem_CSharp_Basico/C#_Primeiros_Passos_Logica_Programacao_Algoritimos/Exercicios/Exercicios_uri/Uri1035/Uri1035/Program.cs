using System;

namespace Uri1035
{
    class Program
    {
        static void Main(string[] args)
        {
            int A, B, C, D, Soma;

            String[] valores = Console.ReadLine().Split(' ');

            A = int.Parse(valores[0]);
            B = int.Parse(valores[0]);
            C = int.Parse(valores[0]);
            D = int.Parse(valores[0]);

            if (B > C)
            {

            }

            Console.WriteLine("Hello World!");
        }
    }
}
